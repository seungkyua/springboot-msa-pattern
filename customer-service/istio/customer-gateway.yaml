apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: customer-gateway
  namespace: msa-pattern
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "customer-service.msa-pattern.com"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: customer
  namespace: msa-pattern
spec:
  hosts:
    - "customer-service.msa-pattern.com"
  gateways:
    - customer-gateway
  http:
    - match:
        - uri:
            prefix: /swagger-ui
        - uri:
            prefix: /customer
        - uri:
            prefix: /v3
      route:
        - destination:
            host: customer-service
            port:
              number: 8082